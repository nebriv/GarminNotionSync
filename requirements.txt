# Pinned for reproducible image builds.
garminconnect==0.3.2
notion-client==3.0.0
python-dotenv==1.2.2
# Decodes activity FIT files to JSON for the "FIT File" Notion attachment.
garmin-fit-sdk==21.208.0
# Physiology metrics computation (activity_metrics.py).
numpy==2.4.6

# Container runtime: scheduling, notifications, metrics.
APScheduler==3.11.2
apprise==1.11.0
prometheus-client==0.25.0
